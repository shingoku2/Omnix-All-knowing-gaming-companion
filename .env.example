# Gaming AI Assistant Configuration
#
# IMPORTANT: This file is generated automatically by the application.
# On first run, the Setup Wizard will guide you through API key configuration.
#
# API keys are stored SECURELY in an encrypted credential store,
# NOT in this file! This file only stores non-sensitive settings.

# ============================================================================
# AI Provider Configuration
# ============================================================================

# AI Provider Selection
# Options: openai, anthropic, gemini
# Default is anthropic (recommended for gaming assistance)
AI_PROVIDER=anthropic

# ============================================================================
# API Keys (Secure Storage)
# ============================================================================
#
# API keys are stored in an ENCRYPTED credential store at:
#   Windows: Credential Manager (via DPAPI)
#   macOS: Keychain
#   Linux: SecretService / keyring (with encrypted file fallback)
#
# These fields are intentionally left blank - keys are managed through:
#   1. First-run Setup Wizard (appears automatically)
#   2. Settings → AI Providers tab (⚙️ button in the app)
#
# To get API keys, visit:
#   - OpenAI: https://platform.openai.com/api-keys
#   - Anthropic: https://console.anthropic.com/settings/keys
#   - Gemini: https://aistudio.google.com/app/apikey

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Session Tokens (for embedded login)
OPENAI_SESSION_DATA='{}'
ANTHROPIC_SESSION_DATA='{}'
GEMINI_SESSION_DATA='{}'

# ============================================================================
# Application Settings
# ============================================================================

# Global hotkey to toggle the overlay window
# Format: ctrl+shift+g, alt+a, etc.
OVERLAY_HOTKEY=ctrl+shift+g

# Game detection interval (seconds)
# How often to check for running games
CHECK_INTERVAL=5

# ============================================================================
# Overlay Window Settings
# ============================================================================
# These settings are automatically saved when you move/resize the overlay
# You don't need to edit these manually

# Window position (pixels from top-left corner of screen)
OVERLAY_X=100
OVERLAY_Y=100

# Window size
OVERLAY_WIDTH=900
OVERLAY_HEIGHT=700

# Window state
OVERLAY_MINIMIZED=false

# Window opacity (0.0 = fully transparent, 1.0 = fully opaque)
OVERLAY_OPACITY=0.95

# ============================================================================
# Advanced Settings
# ============================================================================
# Additional settings are stored in:
#   ~/.gaming_ai_assistant/keybinds.json  (custom keybindings)
#   ~/.gaming_ai_assistant/macros.json    (macro configurations)
#   ~/.gaming_ai_assistant/theme.json     (appearance themes)
