# Gaming AI Assistant Configuration
#
# IMPORTANT: This file is generated automatically by the application.
# On first run, the Setup Wizard will guide you through configuring your local
# Ollama connection. No cloud API keys are required for the default setup.

# ============================================================================
# AI Provider Configuration
# ============================================================================

# AI Provider Selection
# Default is Ollama (recommended for local, no-cost usage)
AI_PROVIDER=ollama

# Ollama Configuration (Local AI - No API Keys Required)
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama3

# ============================================================================
# API Keys (Not required for default local Ollama setup)
# ============================================================================
# For secured/remote Ollama endpoints, you can still store API keys securely in
# your system keyring via the in-app Settings → AI Providers tab. Leave this
# section untouched unless you are connecting to a secured remote instance.

# ============================================================================
# Application Settings
# ============================================================================

# Global hotkey to toggle the overlay window
# Format: ctrl+shift+g, alt+a, etc.
OVERLAY_HOTKEY=ctrl+shift+g

# Game detection interval (seconds)
# How often to check for running games
CHECK_INTERVAL=5

# ============================================================================
# Overlay Window Settings
# ============================================================================
# These settings are automatically saved when you move/resize the overlay
# You don't need to edit these manually

# Window position (pixels from top-left corner of screen)
OVERLAY_X=100
OVERLAY_Y=100

# Window size
OVERLAY_WIDTH=900
OVERLAY_HEIGHT=700

# Window state
OVERLAY_MINIMIZED=false

# Window opacity (0.0 = fully transparent, 1.0 = fully opaque)
OVERLAY_OPACITY=0.95

# ============================================================================
# HRM (Hierarchical Reasoning Model) Integration
# ============================================================================
# Enable structured reasoning analysis for complex questions
# Provides intelligent reasoning frameworks (puzzle solving, strategy, optimization)
# Note: Current implementation uses templates (no PyTorch required)
HRM_ENABLED=false

# Maximum time for HRM analysis in seconds
# Prevents hangs on extremely complex questions
HRM_MAX_INFERENCE_TIME=5.0

# ============================================================================
# Advanced Settings
# ============================================================================
# Additional settings are stored in:
#   ~/.gaming_ai_assistant/keybinds.json  (custom keybindings)
#   ~/.gaming_ai_assistant/macros.json    (macro configurations)
#   ~/.gaming_ai_assistant/theme.json     (appearance themes)
